// Exact or Prefix gives the name (shadow, soundspace, etc)
// - Exact means the region name MUST be this, Prefix means the region name must begin with this...
// Arg is for each argument IN ORDER, takes 2 parameters:
// - The name of the argument.  For required args, this is "".  For others...example: "a=255", name is "a".
// - Description to be shown in the UI.  For example, "a=255", this might be "Alpha Value"
// Shapes is for which shapes this region is allowed to be, takes 1 parameter
// - a string consisting of Box Sph[ere] or Cyl[inder], which specifies allowed shapes...ex. Shapes("Box Sph");
// if "Shapes" is not present, all shapes are assumed to be allowed.

Exact("soundstream") {
	Shapes("Sph");
	Arg("", "Sound Property Name");
	Arg("", "Min Distance Divisor");
}
Exact("soundstatic") {
	Shapes("Sph");
	Arg("", "Sound Property Name");
	Arg("", "Min Distance Divisor");
}
Exact("soundspace") {
	Shapes("Box");
	Arg("", "Sound Space Name");
}
Exact("soundtrigger") {
	Arg("", "Region Name");
}
Exact("foleyfx") {
	Arg("", "Group I.D.");
}

Prefix("shadow") {
	Arg("directional", "Sun Intensity (0 to 1)");
	Arg("ambient", "Ambient Intensity (0 to 1)");
	Arg("color", "Ambient Color (R,G,B)");
	Arg("colortop", "Top Amb. Color (R,G,B)");
	Arg("colorbottom", "Bottom Color (R,G,B)");
}

Exact("rumble") {
	Arg("", "Rumble Class");
	Arg("", "Particle Effect");
}

Prefix("reflection") { 
	Shapes("Box Cyl");
}

Prefix("rainshadow") { 
}

Prefix("danger") { 
}

// more?